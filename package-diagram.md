```mermaid

packageDiagram
    %% Presentation Layer
    package "Presentation Layer (Vue)" {
        package "pages" {
            class DashboardPage
            class ProductListPage
            class MockupEditorPage
            class OrderListPage
            class LoginPage
        }
        package "components" {
            class MockupCanvas
            class ImageUploader
            class DataTable
            class StatusBadge
        }
    }

    %% Application Layer (Logic Controller nằm ở Frontend)
    package "Application Layer (Frontend Logic)" {
        package "services" {
            class AuthService
            class ProductService
            class MockupService
            class PrintAreaService
            class OrderService
        }
        class SupabaseClient
    }

    %% Data Layer
    package "Data Layer (Supabase BaaS)" {
        package "Authentication" {
            class AuthAPI
            class UserSession
        }
        package "PostgreSQL (Database)" {
            class products_table
            class mockups_table
            class print_areas_table
            class variants_table
            class orders_table
        }
        package "Storage (Buckets)" {
            class mockup_images
            class design_files
        }
        class AutoGeneratedAPI
    }

    %% Dependencies
    "Presentation Layer (Vue)" ..> "Application Layer (Frontend Logic)" : uses
    "Application Layer (Frontend Logic)" ..> "SupabaseClient" : uses
    "SupabaseClient" ..> "Data Layer (Supabase BaaS)" : interacts via API
```
